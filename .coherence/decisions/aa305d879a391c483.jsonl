{"id":"d-061e717c","session":"aa305d879a391c483","at":"2026-08-02T22:34:06.454Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"join guard→organ through the graph's OWN file→component parent chain (fileParent), not a longest-prefix match on the symbol's path","over":["re-implementing longest-matching-dir-prefix over component dirs, as the brief described the join"],"because":"measured both on hoist: 14/14 crossings resolve to the same owner either way (requireAuth/OwnedScope/enforceQuota/deleteCredential/serveJson→hoist-chat, 4→auth, 2→env, api→web, Patient→Patient, runWithRetry→model). The graph ALREADY assigns every file to a component via node.parent and buildMap already reads it for symbolsByDir; prefix matching would be a SECOND spelling of component ownership — index-model.ts's rule 1 forbids exactly that"}
{"id":"d-dcbb589c","session":"aa305d879a391c483","at":"2026-08-02T22:34:13.421Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"the perimeter/interior split is UNREAD, not 0/N, when no atlas record exists — measured: coherence's own project has 4 components and NO atlas reading at all","over":["printing '4 of 4 components own no crossing' on a project with no atlas"],"because":"query: jq '.atlas' .coherence/status.json → null here, while hoist has 14 crossings. Saying '0 own a crossing' when nothing was read is green-by-absence in the exact shape floor.ts exists to refuse. The roster reads mp.atlas === null and mp.crossings.total === 0 as two DIFFERENT sentences"}
{"id":"d-1a23aa09","session":"aa305d879a391c483","at":"2026-08-02T22:34:17.754Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"roster/diagram selection is ONE radio group over organs AND guards, with absolutely-positioned HTML <label> hotspots overlaying the SVG's already-computed label plates","over":["SVG <a xlink:href> + :target (collides with the tab mechanism, which is also :target)","checkbox-per-organ (multi-select makes 'dim the rest' meaningless)","clicking only roster chips (that is one direction, and the brief asked for both)"],"because":"the diagram already computes each guard label's rect (lx, ly, lw, LABEL_H) to place it; reusing those numbers as overlay coordinates costs no new derivation, and <label for> is the only no-script way to make an arrow clickable since <label> is not valid inside SVG"}
{"id":"d-1ca55d4b","session":"aa305d879a391c483","at":"2026-08-02T22:48:09.127Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"the roster's order key is the OWNED-CROSSING COUNT on a stable sort, so spec-tree order survives inside each band","over":["alphabetical (orders by an accident of naming)","a hard two-band partition that re-sorts each band by label"],"because":"on hoist it yields hoist-chat 5, auth 4, env 2, then web/Patient/model at 1, then the 8 interior in spec-tree order — reading down IS reading how much of the trust perimeter each organ holds. A stable sort on one key means the split is the ONLY thing this reorders, so the components drill table's long-standing order is not silently rewritten too"}
{"id":"d-dfaaa05d","session":"aa305d879a391c483","at":"2026-08-02T22:48:14.404Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"an unresolvable guard gets owner:null + a WHY, in two named shapes (no such symbol / AMBIGUOUS across N components) — never an arbitrary owner","over":["falling back to the entry component, which the brief's prefix rule does for root-owned symbols","one generic 'unowned' with no reason"],"because":"measured: 14/14 land on hoist and 0/0 on coherence (no atlas), so NEITHER degeneration has a live instance in either project — the mechanism is built anyway because attributing a DANGLING chokepoint to the entry component would inflate exactly the organ that already holds the most perimeter (5 of 14), and that is a silent overstatement of the thing the roster exists to report. Re-run: jq '.map.crossings.shown[]|select(.owner==null)' index.json"}
{"id":"d-6c612641","session":"aa305d879a391c483","at":"2026-08-02T22:48:23.376Z","kind":"conjecture","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"hoist's roster splits 6/8 perimeter/interior, but ALL FOUR of the interior's biggest organs (elicitation, handout, clipboard, search) reach the network or the model — and 8 of 14 owning zero graded crossings looks high for a system whose whole subject is a patient's data","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","the atlas config genuinely only declares crossings at the 6 doors, and the other 8 reach the outside THROUGH them (correct, and the roster is telling the truth)","the atlas config under-declares: shared/search does standalone HTTP to Anthropic and NCBI with no crossing declared for it, which would be a real hole the roster has now made visible for the first time"],"discriminatedBy":"read hoist's atlas config against shared/search and shared/handout: if either performs egress that no declared crossing covers, the interior band is under-counting the perimeter rather than describing it"}
{"id":"d-c67fd6e3","session":"aa305d879a391c483","at":"2026-08-02T22:48:47.426Z","kind":"resolution","agent":"general-purpose","job":"main","branch":"main","commit":"b347169","dirty":true,"chose":"the atlas under-declares — candidate 2. The 6/8 split is honest about what the atlas RECORDED and the roster is what made the gap legible: 'search — each tool wraps STANDALONE HTTP calls' now prints two inches under the heading 'INTERIOR — owns none'. Left for hoist's owner; nothing about the harness changes","over":[],"because":"read hoist's atlas config against the code: 14 transitions, and the ONLY crossing into model-provider is runWithRetry (owned by shared/model). But shared/search/search.ts:56 fetches Anthropic's /v1/messages directly and shared/search/pubmed.ts:71,78 fetch NCBI E-utilities — outbound egress from a component in the INTERIOR band, to a host (NCBI) that is not even a charted region, while the model-provider chart asserts it is 'the app's only outbound egress'","supersedes":"d-6c612641"}
