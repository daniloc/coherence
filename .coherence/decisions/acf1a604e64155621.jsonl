{"id":"d-0d697d90","session":"acf1a604e64155621","at":"2026-08-03T05:36:46.496Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"1162dae","dirty":false,"chose":"build ONLY the neglect reading (commits-since-last-run) as a SessionStart section — not the unwired-input reading, not the gate-adoption reading","over":["a three-reading 'coherence work is due' section covering neglect + unwired inputs + unadopted gates","a new `coherence due` command"],"because":"re-measured all three after today's fixes, per BEFORE YOU ADD A CONCEPT COUNT ITS INSTANCES. NEGLECT has plural subjects on both repos: coherence economy 45 commits / drift 45 (mass 2, atlas 2, verify 1); hoist economy 28 / mass 11 / full-tier 9 (verify 1, atlas 1). UNWIRED INPUTS now has ZERO subjects: hoist .coherence/read-traces/ holds a2875153efa060fa7.jsonl (2733 bytes) and coherence holds one trace + one calibration sample, so the condition the reading would detect no longer exists anywhere — it was a one-shot config bug, fixed, and a detector for it would fire never. UNADOPTED GATES is not representable: coherence is 6-of-7 with contracts reasoned N/A, and there is no config key distinguishing 'not adopted' from 'reasoned not applicable', so on any consuming project it would fire permanently and non-actionably — the dilution the brief forbids. QUERY to re-run: for each status.json section, `git rev-list --count <section.commit>..HEAD`; for unwired inputs, `ls .coherence/read-traces`"}
{"id":"d-05d2dc8a","session":"acf1a604e64155621","at":"2026-08-03T05:38:13.548Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"1162dae","dirty":true,"chose":"a section that has NEVER run is silent — only a section with a recorded run can be reported stale","over":["reporting 'economy has never run' for absent status.json sections"],"because":"an absent section is ambiguous three ways: never ran, does not record, or not applicable to this project. Reporting the first would fire permanently and non-actionably on every consuming project that has no reason to run drift or economy — dilution of the highest-traffic block, the one failure mode the brief names as fatal. It also collapses into the unadopted-gates reading I already declined for being unrepresentable. Neglect as defined is 'the tree moved while the instrument did not' — that presupposes the instrument moved once. Never-ran is unadoption, a different finding with no signal here"}
{"id":"d-5cf52b94","session":"acf1a604e64155621","at":"2026-08-03T05:38:13.800Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"1162dae","dirty":true,"chose":"append the due section at the hook's emit site, keeping agentInstructions() a pure clock-free function","over":["adding the section inside agentInstructions()"],"because":"agentInstructions is printed verbatim by `coherence hooks` (hooks.ts:310) and asserted byte-wise by test/conjecture.test.ts. Making it git-dependent would make `coherence hooks` output vary by repo state and by day — the exact hazard commands.ts documents at 'NO TIMESTAMP IN THE RENDERED BLOCK, on purpose … every normalization a freshness gate needs is a hole in that gate'. Composing at the emit site keeps the pure function pure and the impure reading isolated and separately testable"}
{"id":"d-54c1e4be","session":"acf1a604e64155621","at":"2026-08-03T05:44:37.063Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"1162dae","dirty":true,"chose":"baseline due.ts's two html-value sink rows rather than reword the emitted text","over":["removing the `\"<what you did not do>\"` angle-bracket placeholders so the heuristic stops matching"],"because":"lint-sinks' html-value heuristic fires on interpolation near angle brackets; due.ts:264 is TERMINAL PROSE handed to an agent's context, never rendered as HTML, and the identical construction in hooks.ts's agentInstructions is already baselined for the same reason (3 hooks.ts rows in public/sinks-baseline.json). Rewording to appease the heuristic would degrade the instruction — the placeholder convention is what the other three imperatives in the same block use — for zero safety"}
{"id":"d-e3b208f1","session":"acf1a604e64155621","at":"2026-08-03T05:44:48.457Z","kind":"decision","agent":"general-purpose","job":"main","branch":"main","commit":"1162dae","dirty":true,"chose":"531 lines (2 files, 8 symbols) buys the run record a READER — the first thing that reads status.json's provenance stamps back","over":["leaving the stamps unread and the neglect invisible"],"because":"every section of .coherence/status.json has stamped its own commit since v0.10, and nothing ever asked whether those stamps were still current. Measured today: coherence economy 45 commits behind and drift 45; hoist economy 28 and mass 11. src/due.ts (307 lines) is the reading, test/due.test.ts (224) is 14 tests of which 8 are NEGATIVE — the property being bought is SILENCE on a current tree, because this text joins the SessionStart block that reaches every adopting project on repin. Growth is in Harness core (+307) and Executable contracts (+224), which is the ratio a mechanism whose main risk is firing too often should have"}
{"id":"d-aca60e9b","session":"acf1a604e64155621","at":"2026-08-03T14:14:19.018Z","kind":"decision","agent":"general-purpose","job":"wip/session-start-work-order","branch":"wip/session-start-work-order","commit":"2821b2e","dirty":true,"chose":"re-measured after v0.27.0 landed: the NEGLECT reading still has 5 subjects across 2 repos, so it ships; the other two readings stay declined and v0.27.0 strengthens the second decline","over":["rebuilding the unwired-input reading now that hooks install exists","abandoning the work order because the earlier instances were fixed"],"because":"QUERY (re-run it): for each stamped section of .coherence/status.json, `git rev-list --count <section.commit>..HEAD`; for the tier reading, `git rev-list --count --since=<verify.lastFullAt> HEAD`. AT 2821b2e / hoist 52a2857 — coherence: economy 49, drift 49 (verify 1, atlas 1, mass 0). hoist: economy 29, mass 12, verify-full-tier 10 (verify 1, atlas 1). Five subjects at or over the threshold of 10, on both repos, four days after the same sections were last measured at 45/45/28/11 — the neglect did not shrink while attention was elsewhere, which is the entire claim. UNWIRED INPUTS is now not merely zero-subject but OWNED BY A BETTER MECHANISM: control.ts's inspectLifecycleHook makes a half-wired hook read FALSE rather than absent, which is the same absent-vs-unreadable fix done structurally at install time instead of guessed at from a session hook"}
{"id":"d-984cd4e1","session":"acf1a604e64155621","at":"2026-08-03T14:14:30.558Z","kind":"decision","agent":"general-purpose","job":"wip/session-start-work-order","branch":"wip/session-start-work-order","commit":"2821b2e","dirty":true,"chose":"kept the full-tier sub-reading, which had ZERO subjects at the threshold when I wrote it and has one now","over":["cutting it under BEFORE YOU ADD A CONCEPT COUNT ITS INSTANCES, as I cut the other two readings"],"because":"honest accounting: when I built it hoist sat at 9 commits since lastFullAt against a threshold of 10 — under my own rule that is a mechanism with no subjects and I should have cut it. I kept it on a STRUCTURAL argument rather than a count, and the count has since crossed: hoist is now at 10 and fires. The structural argument is that hoist's habit makes this permanent, not transient — `npm run verify` is `--fast` and `verify:full` is pre-deploy only, so the full tier drifts behind BY DESIGN there, and it is the one case where the section's own stamp reads fresh while every boundary oracle has been skipped. Reporting verify as current on the strength of a run that graded almost nothing is green-by-absence in the newest instrument. A future agent who finds this firing constantly on one project and never elsewhere should re-run: `git rev-list --count --since=<verify.lastFullAt> HEAD` and consider whether the threshold should differ per tier"}
{"id":"d-6affaf4a","session":"acf1a604e64155621","at":"2026-08-03T14:14:56.356Z","kind":"retraction","agent":"general-purpose","job":"wip/session-start-work-order","branch":"wip/session-start-work-order","commit":"2821b2e","dirty":true,"chose":"(withdrawn: d-e3b208f1)","over":[],"because":"the figures in it were measured against a baseline the v0.27.0 rebase superseded, and the re-pin it authorized was discarded with it. It said 531 lines / due.ts 307; the true deltas against the current baseline are +522 total, due.ts 281 + 17 lines of hooks.ts wiring = +298 Harness core, test/due.test.ts 224 = +224 Executable contracts. A journal entry carrying a stale number is worse than no entry — superseded by the accurate re-pin recorded next","supersedes":"d-e3b208f1"}
{"id":"d-b0ec0593","session":"acf1a604e64155621","at":"2026-08-03T14:14:56.617Z","kind":"decision","agent":"general-purpose","job":"wip/session-start-work-order","branch":"wip/session-start-work-order","commit":"2821b2e","dirty":true,"chose":"+522 lines / 2 files / 8 symbols buys the run record its FIRST READER — the provenance stamps have been written since v0.10 and never once read back","over":["leaving status.json's per-section commit stamps unread, so neglect stays invisible until somebody happens to look"],"because":"MEASURED SUBJECTS at 2821b2e: coherence economy 49 commits behind, drift 49; hoist economy 29, mass 12, verify's full tier 10. Five real subjects on two repos. THE SPLIT IS THE POINT: 281 lines of src/due.ts (+17 wiring hooks.ts) against 224 lines of test/due.test.ts, and 8 of those 14 tests are NEGATIVE — silence on a current tree, on a quiet tree however old the run, on a section that never ran, on an uncountable distance, on a corrupt record, on a name that is not a command. The property being bought is SILENCE, because this text joins the SessionStart block that reaches every adopting project on repin and already carries three imperatives; a fourth that fired every session would cost the other three their attention. 8 symbols: DUE_AFTER, DUE_CAP, DueItem, DueReading, commitsSince, commitsSinceTime, readDue, formatDue"}
