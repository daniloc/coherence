{"id":"d-35a9a1d1","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-07-31T23:59:11.064Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"10913ee","dirty":true,"chose":"docs --check reported 4 stale artifacts (AGENTS.md, graph.json, _graph.html, _overview.html) on a byte-identical checkout of the SAME commit, verified in a git worktree whose directory basename differed from the main checkout's","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","the project name is derived from the directory BASENAME, so every generated artifact embeds it and any checkout under a different directory name reads as stale — meaning docs --check is not hermetic and would fail in any CI that clones to a differently-named path","the artifacts were genuinely stale and the name substitution merely masked a real delta"],"discriminatedBy":"regenerate in the renamed worktree and diff: if the ONLY changed lines are the name substitution plus the generatedAt timestamp, the derivation is the cause (CONFIRMED — that is exactly what the diff showed)"}
{"id":"d-0f862dab","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-07-31T23:59:48.957Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"10913ee","dirty":true,"chose":"the instrument was wrong — basename derivation, artifacts current","over":[],"because":"derive.ts:98 sets root: basename(resolve(root)) with no config override anywhere — the directory basename is baked into every generated artifact (AGENTS.md title, the Structure tree, graph.json, both HTML renders). Regenerating in the renamed worktree produced a diff containing ONLY the name substitution and the generatedAt timestamp, so the four artifacts committed at 10913ee are genuinely current. The staleness was my instrument, not the tree. Note the blast radius is narrow in practice: a project whose cfg.root is a subdirectory (hoist roots at app/) gets a stable basename regardless of clone path; it is a repo-rooted project cloned to a non-default directory name that would break","supersedes":"d-35a9a1d1"}
{"id":"d-96492391","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T00:01:04.839Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"890f0fd","dirty":true,"chose":"orphaned — confirmed dead, zero consumers and zero tests","over":[],"because":"Verified independently at the committed eviction state (10913ee) in an isolated worktree: grep for withBaseWorktree, outsideTally and deriveOutside across src/, test/ and scripts/ — excluding their own definition site in src/tree.ts — returns ZERO hits. Not merely no product consumers: no TESTS either, so they are dead AND unwitnessed. There is also no library escape hatch, because package.json declares a bin only, with no main, no exports and no src/index.ts barrel, so nothing outside this repo can be importing them. That is 71 of tree.ts's 160 lines (lines 90-160). By contrast fileStats and claimedFilePaths are genuinely shared and the extraction was correct for them: fileStats has consumers in economy.ts, mass.ts and promise.ts, claimedFilePaths in promise.ts. Scheduled for deletion in a follow-up commit rather than folded into the eviction commits, which are landed and verified","supersedes":"d-34e66b04"}
{"id":"d-afd7f50f","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T00:04:19.586Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"890f0fd","dirty":true,"chose":"Treat regenerated-artifact drift found after a mutation-testing session as suspect-until-regenerated, and regenerate rather than reading the diff as a regression","over":["Filing the C/stale-to-D/unknown gate drop in public/promise.json as a regression introduced by the evictions","Reverting the dirty artifacts to their committed state"],"because":"public/promise.json and _contract.html were left dirty in the main checkout showing 13 gates dropping from grade C/stale to D/unknown — history apparently vanishing, which is the exact shape of the claimKey erasure bug (d-655d524a). It was not one. Isolated regeneration at both 10913ee and 890f0fd in clean worktrees produced ZERO unknowns (13 and 14 stale respectively), and the claim record set GREW 27 to 28 across the evictions rather than shrinking. Regenerating in the main checkout now yields 14 gates all fresh/pass. The unknown verdicts were written while the floor agent's mutation test had buildGraph gutted, so the graph genuinely carried no claims at that instant and the artifact faithfully recorded a mutated tree. THE GENERAL HAZARD, which is what makes this worth recording: a mutation test that runs a command which WRITES generated artifacts leaves those artifacts describing the mutated tree after the mutation is reverted, because reverting the source does not re-run the generator. The artifacts then read as a regression to the next reader. Mutation testing should either run only non-writing commands or regenerate artifacts as its last step"}
{"id":"d-7bfac53e","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T00:07:54.114Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"890f0fd","dirty":true,"chose":"Delete withBaseWorktree, outsideTally and deriveOutside from tree.ts in the same release that orphaned them, preserving their hard-won gotchas as prose in RELEASE-NOTES","over":["Keeping them against a future diff-against-a-base command, as the eviction agent did on scope discipline","Keeping them but marking them deprecated"],"because":"They had zero callers AND zero tests, and package.json declares a bin with no main, no exports and no barrel, so no external consumer could reach them either — nothing would ever have reddened if they rotted. A release whose whole thesis is that unused mass gets removed cannot itself ship 71 lines of dead code; that teaches the opposite of what it is for. Deletion is cheap to reverse because git has the code, but the KNOWLEDGE inside it is not recoverable by someone who does not know to look, so the four non-obvious facts were lifted into the release notes verbatim: throwaway detached worktree with unconditional three-step teardown, subdirectory-root config alignment (or every diff reads as a total razed/arrived storm), realpath on both ends because show-toplevel canonicalizes while cfg.root may be symlinked, and rename entries dropping on either path. tree.ts goes 160 to 88 lines; 581/581 and verify 28/28 green after"}
{"id":"d-096a4b77","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T01:39:59.454Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"d27a122","dirty":true,"chose":"The non-vacuity floor now guards the six artifact GENERATORS, but not the ratchets that write BASELINES (mass, conventions, lint-sinks, contracts) under --update-baseline — so a broken deriver could still be baselined, pinning a near-zero surface as the new floor and silently disarming the ratchet","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","the exposure is real but small, because --update-baseline is an explicit human act that leaves a reviewable diff, whereas a generator runs unattended in pre-commit hooks and CI — which is exactly why the generators were guarded first","it is worse than the generator case, not smaller: a poisoned artifact is regenerated and self-heals the moment derivation is fixed, but a poisoned BASELINE is sticky by design and every later run compares against it, so the damage compounds instead of washing out"],"discriminatedBy":"run each ratchet's --update-baseline against a gutted buildGraph in a fixture and read the written baseline: if the dimensions collapse to zero or vanish rather than the command refusing, the hazard is confirmed and the floor should extend to writesBaseline the same way it extended to writesArtifacts"}
{"id":"d-0c984407","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T01:42:53.950Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"b32965d","dirty":false,"chose":"Fix the docs hermeticity defect by DECLARING the project name in config, and extend the non-vacuity floor to every artifact-writing command via a registry flag enforced by a behavioral totality oracle","over":["Leaving the basename derivation and documenting the worktree gotcha","Normalizing the name away in the staleness comparison, the way generatedAt and absRoot already are","A hand-written generator list in cli.ts instead of a registry flag"],"because":"Both findings were the same defect shape: something environment-dependent leaking into artifacts that are supposed to be a pure function of the tracked tree. Normalizing the name away was rejected because every normalization a freshness gate needs is a hole in that gate — commands.ts says so about its own block, and cli.ts already carries three in normGraphHtml. A hand-written list in cli.ts was rejected because a second spelling of a domain is precisely the drift commands.ts exists to end. Verified end-to-end: a worktree literally named totally-different-name now reports docs current, exit 0, where the old code reported four byte-correct artifacts stale. Both fixes are mutation-checked — reverting the guard reds only the generator test, reverting the name fix reds only the hermeticity test — and the derive suite leads with a NEGATIVE CONTROL that varies the path with no name declared and asserts divergence, because a stability test that never varies the path would have passed against the old hard-coded basename too"}
{"id":"d-80db2bb4","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T01:59:10.206Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"e67ad2a","dirty":false,"chose":"worse than the generator case — sticky, inverting, and already silent at --check","over":[],"because":"Ran the discriminating test on a fixture at v0.23.0. The compounding candidate won, and the damage starts EARLIER than --update-baseline. With buildGraph gutted: (1) mass --check exits 0 and prints '✓ mass ratchet held', then actively prescribes the poisoning — '4 dimension(s) SHRANK since the pin (never a failure) — re-pin to bank it'. So a total derivation collapse is not merely undetected, the tool CONGRATULATES it and recommends banking it. (2) --update-baseline then pins files|total=0, lines|total=0, symbols|total=0, undocumented|symbols=0, and silently DROPS lines|app entirely (5 dimensions to 4). (3) Restoring derivation does not self-heal: mass --check now exits 1 reporting the project's own pre-existing mass as GROWTH ('files|total grew 0→2'), demanding a coherence decide justifying mass that was always there. The poisoning INVERTS rather than washes out, and its recovery message misdiagnoses. Also learned: the three baseline writers do not share a vacuity source — mass and conventions read the graph, but lintSinks(cfg, mode) never takes a graph and reads scanSources, so the verify floor's 'graph has no claims' predicate is the wrong instrument for it. contracts has no update mode and is not exposed","supersedes":"d-096a4b77"}
{"id":"d-684f5e74","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T02:35:44.730Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"e67ad2a","dirty":true,"chose":"Treat the poisoned baseline as one instance of GREEN-BY-ABSENCE — an instrument rendering a verdict without the population it read — and enforce the denominator with a registry-enumerated degenerate-project oracle rather than fixing the ratchets alone","over":["Fixing mass/conventions/lint-sinks in isolation as a baseline bug","Adding a denominator to each report by convention, documented but unenforced"],"because":"Surveyed all 14 checking instruments against a degenerate project (valid config, a spec, ZERO code files, not a git repo). Measured three sub-shapes of one defect. VACUOUS GREEN: why-lint --check prints '✓ no ## why sentence restates an anchored chokepoint/oracle mechanism' over zero why sentences, exit 0; prose prints '✓ no duplicated prose above the floor'; mass prints '✓ mass ratchet held' over a collapsed reading and then prescribes re-pinning it. DENOMINATOR-LESS NULL: decompose says 'no decomposition smells surfaced' without saying over what. ILLEGIBLE REFUSAL: signal --check throws an unhandled Error from structural.ts:192 with a stack trace and a Node version banner instead of a sentence. CRUCIALLY the cure already exists in this codebase five times over and was simply not generalized — drift says 'only 0 mapped development commits', economy says 'no closures to measure: nothing in the last 400 commits touched a file the graph owns', calibrate says 'no samples yet', lint-sinks prints 'total reviewed surface: 0', conventions prints '0 candidate convention(s)', and atlas/contracts decline outright with 'no config — nothing to check'. So this is the repo's own redundancy doctrine turned on itself: derive one spelling from the ones that are already right. The enforcement is a permanent degenerate-project fixture enumerating COMMANDS, asserting no instrument renders a health glyph where nothing was examined and none emits an unhandled stack trace — the same shape as the writesArtifacts oracle, so a new command is covered the day it is registered"}
{"id":"d-bab08665","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T03:16:58.709Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"25c543e","dirty":true,"chose":"Fix all three adversarial-review findings rather than accepting any as known limitations, and record in each test's header WHY the gap existed","over":["Deferring the README blind spot because no current generator writes only there","Treating the vacuity oracle's missing instrument check as theoretical, since it only opens when the CLI front door breaks"],"because":"Two fable reviewers found three real defects by mutation, each reproduced in both directions. (1) test/vacuity.test.ts was ITSELF vacuous: both its assertions are universal quantifiers over command output with no check that any command ran, so inserting process.exit(2) after cli.ts's cmd read made both tests pass green — in the one file whose entire subject is instruments that cannot distinguish finding nothing from not looking. The idiom was already in this suite twice (commands.test.ts live.length>=20, and observed.length>=3 in both totality oracles) and simply was not copied. Writing the rule down is not obeying it. (2) claimedFilePaths lost its only witness when scene.test.ts was evicted: mutating it to bless every ambiguous candidate — the exact over-report it exists to prevent — passes 593/593 with verify ✓ coherent, because no spec anchor names it. The asymmetry is the lesson: the UNDER-report direction reds a promise.test.ts assertion, so only the direction that silently inflates a coverage number went dark. (3) artifactSnapshot watched AGENTS.md/CLAUDE.md/public but not README.md, which docs also writes, so a future root-markdown-only generator would evade the writesArtifacts oracle. THE GENERAL LESSON, which the eviction audit missed: auditing an eviction by TEST COUNT answers arithmetic, not 'was any surviving guarantee ONLY witnessed there'. The second question is the one that bites"}
{"id":"d-ca90fb5d","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T16:34:58.896Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"74875a5","dirty":true,"chose":"tsc --noEmit passes on this repo while a test file references a completely undefined identifier — tsconfig.json's include is [\"src/**/*.ts\"], so 611 tests' worth of code is never typechecked","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","deliberate and harmless: the node:test suite executes every test file, so an undefined identifier is caught at runtime within seconds, and typechecking tests would slow the inner loop for a class of error the suite already catches loudly","a real gap: type errors that do NOT throw at runtime — a wrong-shaped fixture, a stale type import, an assertion comparing incompatible types that happens to pass — are invisible in exactly the files whose job is to be evidence, and this repo's whole thesis is that evidence must itself be checked"],"discriminatedBy":"add test/**/*.ts to include and run tsc --noEmit: count the errors. Zero means the runtime suite really was sufficient; a nonzero count is a list of type defects sitting in the evidence layer right now. I hit this live — an import that failed to patch left neverRedRank undefined and tsc reported clean"}
{"id":"d-5d1ca80b","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-01T16:36:09.147Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"74875a5","dirty":true,"chose":"a real gap — 3 live instances, and they bypass a tier-1 brand","over":[],"because":"Ran the discriminating test. First attempt was an instrument failure — adding test/**/*.ts to include produced 40 errors, ALL TS6059 (file not under rootDir: src), a config error measuring nothing. Relaxing rootDir gives the real answer: 3 genuine type errors, all TS2345, all in test/promise.test.ts, all the same shape — Map<string, ClaimRecord> passed where Map<ClaimKey, ClaimRecord> is required. That is not incidental. ClaimKey was BRANDED (boundary.ts:65) precisely so an unnormalized map becomes a compile error, after the silent history-erasure bug where mergeClaimRecords keyed sticky history on the raw claim string while claimKey normalized the crossing clause; indexClaimRecords() is meant to be the one map-mint. The brand is enforced across src and NOT across test, so the evidence layer can construct exactly the illegal state the brand exists to forbid — and it is the layer asserting the behavior is correct. A tier-1 unrepresentability that stops at the test boundary is enforced in the half that cannot lie and unenforced in the half that can","supersedes":"d-ca90fb5d"}
{"id":"d-67ba8ed4","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-03T05:50:39.153Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"f49fd3c","dirty":true,"chose":"Re-pin mass and conventions for the control interface: +948 lines, +2 files, +22 symbols, +1 claim, and three new fan-out guards accepted as debt","over":["Converting isObject/canonicalGroup/canonicalAction into contracts in the same change","Leaving the baselines stale so the ratchets stay red"],"because":"What the new mass BUYS is the elimination of hand-wired lifecycle hooks. Measured today in the consuming project: PostToolUse was bound to one repo-local script on Edit|Write, coherence's own hook was never added, and recordHookReads therefore never ran — so calibrate sat implemented with zero input for the life of the feature and nothing anywhere said so. That is 948 lines to convert a paste-this-block instruction into an installed, canonical, verifiable bundle whose control bit is false when the path is not singular. The three new conventions are guards with fan-out and no contract — isObject (11 sites) is a JSON-shape narrowing used throughout the settings reader, canonicalGroup (3) and canonicalAction (2) classify hook entries during migration. They are pinned as debt rather than converted because a contract for them wants the settings schema to be a declared type, which is a separate change with its own oracle obligation, and doing it inside a release would trade a checked ratchet for an unchecked refactor. Also charted inspectLifecycleHook as project-source to verdict, closing the atlas drift the new self-claim opened"}
{"id":"d-0ab690e4","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-03T14:34:26.551Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"f957b7b","dirty":true,"chose":"Treat coherence's three tier-3 red crossings as NOT security concerns under the actual deployment, and record that the atlas security flag is over-applied here rather than acting on the alarm","over":["Writing boundary claims for buildGraph, loadConfig and readJsonOrRefuse to clear the red","Leaving the alarm unexamined because the figure renders it loudly"],"because":"Measured: 14 of 18 crossings carry security:true, so the flag fires on 78% of the graph and distinguishes almost nothing — it reads as 'this is a crossing', not 'this is a risk'. Taken individually under the real deployment, a CLI an owner runs in their own tree: loadConfig (project-source -> shell) is the only genuine execution surface, and it is exactly the trust level of npm test running package.json scripts — if you run the tool in a tree you already execute that tree's code, so it adds no exposure. buildGraph reads repo text into a model and executes nothing; its real failure mode is VACUITY, already governed by the non-vacuity floor, which is a correctness gate not a security one. readJsonOrRefuse reads the tool's own status.json and baselines, where the risk is corruption rather than attack, and v0.24.0 already fixed that by separating absent from unreadable. The threat model in which all three become real is pointing coherence at a repo you do NOT trust — an unfamiliar PR, a dependency under evaluation, an agent cloning something arbitrary. That has ZERO instances across three adopters, all owned by the same person. So the red is technically accurate (tier-3, no boundary claim, on a security-flagged crossing) and the ALARM is not. DISCRIMINATOR, so this can go stale honestly: if coherence is ever run against a tree its operator did not write, re-open this — loadConfig becomes arbitrary code execution from untrusted config and earns a boundary claim immediately. Until then, writing three claims to clear a red would be manufacturing evidence for a threat with no instances"}
{"id":"d-173c7206","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-03T14:35:55.879Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"6ff22b6","dirty":true,"chose":"split — tier is real and varies by codebase; the SECURITY flag is what carries no information","over":[],"because":"Measured on a SECOND project and the two halves of the question separate. TIER VARIES BY CODEBASE, so the encoding is real: coherence's own atlas is 15 tier-2 + 3 tier-3 with NO tier-1, while hoist is 1 tier-1 + 17 tier-2 with NO tier-3. Two projects, two genuinely different shapes — the near-constant on hoist was a property of hoist, not of the instrument, and the figure is right to give the rare tier the loud treatment because on some project it is the rare one. SECURITY SATURATES IN BOTH, so that flag is the one carrying almost no information: coherence 14 of 18 (78%), hoist 16 of 18 (89%). A flag that fires on four crossings in five reads as 'this is a crossing', not 'this is a risk' — which surfaced when a direct challenge asked whether coherence's three red ingress doors are really a concern for a CLI, and the answer was no (see d-0ab690e4: loadConfig is the trust level of npm test running package.json scripts, buildGraph's real failure mode is vacuity and the floor already governs it, readJsonOrRefuse's is corruption and v0.24.0 answered it). So: keep the tier encoding, and treat the security flag as an authoring default that has not been exercised as a distinction. What would settle THAT: a project whose atlas marks fewer than half its crossings security — none exists across three adopters","supersedes":"d-759f7107"}
{"id":"d-7d19aafe","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-03T14:36:16.769Z","kind":"retraction","agent":"main","job":"main","branch":"main","commit":"6ff22b6","dirty":true,"chose":"(withdrawn: d-173c7206)","over":[],"because":"The figure for hoist was wrong. I wrote '16 of 18 (89%)' from memory before the measurement printed; the actual count is 12 of 18 (66%). The conclusion is unchanged in direction but the evidence in the entry was not what I measured, and a journal entry carrying a stale number is worse than no entry — the same reason d-e3b208f1 was retracted earlier today. Re-filed with the measured figures","supersedes":"d-173c7206"}
{"id":"d-abb227ef","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-03T14:36:17.938Z","kind":"resolution","agent":"main","job":"main","branch":"main","commit":"6ff22b6","dirty":true,"chose":"split — tier is real and varies by codebase; the SECURITY flag is the one carrying little information","over":[],"because":"Measured on a SECOND project and the two halves separate. TIER VARIES BY CODEBASE, so that encoding is real: coherence is 15 tier-2 + 3 tier-3 with NO tier-1; hoist is 1 tier-1 + 17 tier-2 with NO tier-3. Two projects, two genuinely different shapes — the near-constant observed on hoist was a property of hoist, and the figure is right to give the rare tier the loud treatment, because which tier is rare differs by project. SECURITY is the flag carrying little information, though less starkly than the original observation suggested: coherence 14 of 18 (78%), hoist 12 of 18 (66%) — both measured just now, not recalled. Two thirds to four fifths is saturated enough that the flag reads closer to 'this is a crossing' than 'this is a risk', which is what a direct challenge exposed when it asked whether coherence's three red ingress doors are really a concern for a CLI: they are not (d-0ab690e4 — loadConfig is the trust level of npm test running package.json scripts; buildGraph's real failure mode is vacuity, already governed by the floor; readJsonOrRefuse's is corruption, answered in v0.24.0). KEEP the tier encoding. Treat the security flag as an authoring default that has never been exercised as a distinction. What would settle that half: an atlas marking fewer than half its crossings security — none exists across three adopters, so it stays a suspicion rather than a finding","supersedes":"d-759f7107"}
{"id":"d-a11a593e","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:01:53.508Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"9e00072","dirty":false,"chose":"a ratchet pinned a baseline derived from an artifact a SIBLING gate had already condemned: hoist ran `mass --update-baseline` while `check-lockfile` was red, so the pin captured a lockfile missing a package and the later repair read as growth","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","correct and unavoidable — mass measures the tree as it is, and it is the operator's job not to pin during a known-red state; the ratchet has no business knowing what other gates think","a real ordering hole — `--update-baseline` is the one operation that converts the CURRENT state into the future's definition of normal, so it is exactly the operation that should refuse while any gate is red; every other command only reads","narrower than it looks: only measures whose command reads a derived artifact (lockfile, bundle, generated file) can be poisoned this way, and the line/symbol dimensions read source directly and cannot"],"discriminatedBy":"count the mass dimensions across adopters whose cmd reads a DERIVED artifact versus source text. If derived-artifact measures are a small minority, the fix is per-measure (declare a measure as artifact-derived and have it refuse when its producer is unclean), not a global cross-gate interlock. hoist has 2 of 23: worker-bundle and web-assets, both wrangler/vite outputs, plus deps|* which read the lockfile"}
{"id":"d-11acc005","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:23:15.869Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"cb5b032","dirty":false,"chose":"four `coherence resolved` calls that shared identical --because/--as text all minted the SAME id (d-a615bae0), and the settled render kept ONE — three conjectures silently stayed open while the CLI printed a success line for each","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","working as designed: the id is content-addressed so that a replayed or duplicated append is idempotent, and the collision is that mechanism doing its job — the operator's error was writing one reason for four different questions","a real hole in the identity function: the id hashes the resolution's TEXT but not its TARGET, so two resolutions of DIFFERENT conjectures are indistinguishable to it; including the resolved id in the hash costs nothing and makes the collision impossible","the render is the defect, not the id: four distinct rows with the same id DID land in the jsonl, each naming its own target, so the writer is fine and the reader is what drops three"],"discriminatedBy":"read the jsonl directly — measured: 4 rows carry d-a615bae0 with supersedes targets d-d4991c65, d-2344b211, d-ffa5218a, d-2d152250, and only the LAST is reported closed. That already rules out a write-side loss and localizes it to identity-plus-render. Remaining test: hash the target into the id and re-run; if all four close, the id function was the defect. Note the failure is fail-SAFE (questions stay open rather than falsely closing) but it is silent, and the CLI's per-call success line actively says otherwise"}
{"id":"d-77dc0ad1","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:27:41.442Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"7e0945c","dirty":false,"chose":"the never-red advisory cannot tell 'no negative control exists' from 'one exists and nobody transcribed it' — hoist has been running 43 mutations per probe run for weeks while coherence raised every one of those invariants as never-observed-failing, because `## refutations` is prose and prose is the only thing the advisory reads","over":[],"because":"","couldBe":["correct and deliberate: the point of the prose line is that a HUMAN wrote down what a break MEANS, and a machine-readable tally would let a project satisfy the advisory without anyone understanding the failure","a missing input: the advisory has exactly one source, so a project whose evidence lives anywhere else is indistinguishable from a project with no evidence — and the fix is a declared refutation SOURCE (a command emitting invariant -> oracle -> tally) that the advisory reads ALONGSIDE the prose, never instead of it","the advisory is fine and the adopter was wrong to keep two surfaces: the probe should have been writing the spec line all along, and coherence should say so at adoption rather than after 33 have accumulated"],"discriminatedBy":"measured today in hoist: 33 of 43 probed boundaries had no line; writing them moved refutation coverage 4/17 to 43/52 with ZERO change to what the code does or what the probe proves. If a declared source would have surfaced those 33 the day each mutation landed, the advisory was under-informed, not the project. Test it by giving one adopter a `refutations.source` command and comparing how long a new mutation takes to reach the advisory, against hoist's measured latency of weeks"}
{"id":"d-c9cf780b","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:28:00.523Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"7e0945c","dirty":true,"chose":"a refutation line is a cached fact with no expiry, and all ten of hoist's had already rotted: every one quoted a 528-test denominator against a suite that now runs 533","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","harmless — the denominator is incidental colour and only the RED verdict matters, so a stale total misleads nobody","the same rot premise leases already gate: the line asserts 'this oracle went red, and here is the run that says so', and a denominator from a suite that no longer exists means the citation names a run nobody can reproduce — coherence audits decision premises for exactly this and does not audit its own evidence lines","a symptom rather than the disease: the tally is hand-copied because there is no machine path from a probe run to the spec, which is the same gap as the refutation-source question"],"discriminatedBy":"cheap and local: parse each `## refutations` line's quoted total and compare it against the current suite's test count. hoist would have gone red on 10 of 10 before today and 0 of 43 after. If that check is worth a gate, it is worth it for the same reason `premise --check` is — a cached review that expires silently spends a reader's trust on a fact nobody re-earned"}
{"id":"d-e04e6475","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:28:01.761Z","kind":"conjecture","agent":"main","job":"main","branch":"main","commit":"7e0945c","dirty":true,"chose":"regenerating hoist's atlas.md produced a diff whose ENTIRE content was heat drifting 15% to 14% on three crossings — a committed artifact that cannot hold still, changing under a field that is deliberately forbidden from affecting any verdict","over":[],"because":"","couldBe":["the instrument is wrong — the thing that PRODUCED this number, not the thing it describes","working as intended: heat is a temperature a reader should see move, and a diff is how they see it","the artifact is carrying a field that does not belong in version control — heat is a function of the last N commits, so it changes whenever HEAD does, which makes every atlas regen a commit whose review value is zero and whose noise is real","a bucketing problem, not a placement problem: heat is rendered to whole percents over a 200-commit window, so single-commit movement is visible; coarser buckets (the existing glyph alone, or deciles) would hold still across ordinary work and still move when something genuinely heats up"],"discriminatedBy":"count how many of hoist's atlas.md commits changed ONLY heat digits versus changed a tier, chokepoint, oracle or prose. If heat-only churn dominates, the field is costing review attention it never repays, and the glyph — which did NOT change at 15%->14% — is already the honest resolution"}
{"id":"d-16dcb53d","session":"3e2c9d10-82b3-4627-a861-555b53d9777d","at":"2026-08-05T02:28:19.369Z","kind":"decision","agent":"main","job":"main","branch":"main","commit":"7e0945c","dirty":true,"chose":"adopter feedback, 2026-08-04 session: the strongest evidence for the ratchet design this project has produced is that mass --check caught its own OPERATOR — I poisoned a baseline and the instrument, not I, found it","over":["reading the day as five defects found in coherence — three of the five (the id collision, the update-baseline ordering, the never-red advisory's single input) are real, but the day's dominant result is that four separate instruments each caught something a careful human had already waved through","filing this as a conjecture — nothing here is uncertain enough to need a discriminator; it is a report"],"because":"the sequence is worth keeping because it is the argument for the whole apparatus. (1) mass --check fired on a 14-line growth from a commit whose own message listed four gates it had run and omitted the fifth. (2) mass --check fired AGAIN, hours later, on a baseline I had pinned myself while a sibling gate was red — the ratchet noticed the repair of a break it had been taught to call normal. (3) hoist's own pre-commit caught a lockfile npm 10 could not install, printed the failure in full with the repair command, and let the commit through because line 19 lacked the || exit 1 every line below it carried — an alarm wired to nothing, found only because it fired. (4) the never-red advisory kept naming 33 invariants whose negative controls had been running for weeks, which read as a false positive and was in fact the record being honest that nobody had written the evidence down. In every case the instrument was right and the human narration around it was wrong, including mine. Filed alongside: d-11acc005 (resolved ids hash their text, not their target), d-a11a593e (--update-baseline is the one command that defines normal and should refuse while a sibling gate is red), d-77dc0ad1 (the never-red advisory has one input and cannot see a machine-readable negative control), d-c9cf780b (a refutation line is an unexpiring cached fact — hoist's ten had all rotted to a stale denominator), d-e04e6475 (atlas.md churns on heat alone)"}
